import { ogCard, ACCENT, size, contentType } from "@/lib/ogCard";

export const alt = "Get the operator's read before the mistake compounds.";
export { size, contentType };

export default function Image() {
  return ogCard({
    eyebrow: "FOR FOUNDERS",
    title: "Get the operator's read before the mistake compounds.",
    footer: "GTMERS.CO/FOUNDER",
    accent: ACCENT.GOLD,
  });
}
